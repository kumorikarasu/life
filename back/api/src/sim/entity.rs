/*
use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct Sim {
    pub name: String,
    pub stats: Vec<Stat>,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct Stat {
    pub name: String,
    pub value: f32,
}
*/
